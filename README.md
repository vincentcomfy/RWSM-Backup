# RWSM-Backup
Originally created by Rainy, I'm just sharing it here since the original files were taken down.<br>

Make sure to also add these to your config.blk:
<br>
`enable_mod:b=yes`<br>
`useLoopsForGunSound:b=no`<br>
`useLoopsForGMGunSound:b=yes`
